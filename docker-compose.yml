version: '3.1'

services:

  helloworld:
    image: "${AUTHOR}/${NAME}:${TAG}"
    environment:
    volumes:
    secrets:
    deploy:
      replicas: "${REPLICAS}"
      placement: 
        constraints: 
          - "node.labels.env==${ENV}"

secrets: