version: '3.1'

services:

  helloworld:
    image: "${AUTHOR}/${NAME}:${TAG}"
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    deploy:
      replicas: 1
      placement: 
        constraints: 
          - "node.labels.env==${ENV}"